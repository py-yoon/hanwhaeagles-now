import test from 'node:test'; import assert from 'node:assert/strict'; import { runFeatureAblation } from './feature-ablation.js';
test('feature ablation runs online without look-ahead',()=>{const games=[{game_id:'1',date:'2026-01-01',home:'A',away:'B',home_score:2,away_score:1,status:'FINAL'},{game_id:'2',date:'2026-01-02',home:'B',away:'A',home_score:1,away_score:0,status:'FINAL'},{game_id:'3',date:'2026-01-03',home:'A',away:'B',home_score:1,away_score:1,status:'FINAL'}];const r=runFeatureAblation(games,{teams:['A','B']});assert.equal(r.results.length,5);assert.equal(r.results[0].metrics.games,3);assert.ok(r.best_by_log_loss);});
