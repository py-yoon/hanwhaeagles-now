import { winRate } from '../engine/standings.js';
export function normalizeGamesBehind(v){if(v===null||v===undefined||v==='-'||v==='0')return 0;const n=Number(String(v).trim());return Number.isFinite(n)?n:v;}
export function normalizeOfficialSnapshot(rows){return rows.map(r=>({...r,win_rate:Number((r.win_rate??winRate(r)).toFixed(3)),...(r.games_behind!==undefined?{games_behind:normalizeGamesBehind(r.games_behind)}:{})}));}
export function calculateGamesBehind(standings){const top=standings[0];return standings.map(t=>({...t,games_behind:Number((((top.wins-t.wins)+(t.losses-top.losses))/2).toFixed(1))}));}
export function compareOfficialSnapshot(calculated,official){const c=calculateGamesBehind(calculated),o=normalizeOfficialSnapshot(official),om=new Map(o.map(x=>[x.team,x])),mismatches=[];for(const r of c){const e=om.get(r.team);if(!e){mismatches.push({team:r.team,reason:'MISSING_OFFICIAL_TEAM'});continue;}for(const f of ['rank','games','wins','losses','draws','win_rate','games_behind']){if(e[f]===undefined)continue;if(r[f]!==e[f])mismatches.push({team:r.team,reason:`${f.toUpperCase()}_MISMATCH`,calculated:r[f],official:e[f]});}}return{status:mismatches.length?'FAIL':'PASS',teams_checked:o.length,mismatches};}
